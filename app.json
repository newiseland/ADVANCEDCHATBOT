{
  "name": "Telegram Bot",
  "description": "A Telegram bot powered by Python and OpenAI.",
  "repository": "https://github.com/newiseland/ADVANCEDCHATBOT",
  "keywords": ["python", "telegram", "bot", "openai"],
  "stack": "heroku-22",
  "env": {
    "TELEGRAM_BOT_TOKEN": {
      "description": "The API token for your Telegram bot from BotFather.",
      "required": true
    },
    "OPENAI_API_KEY": {
      "description": "The API key for OpenAI to access GPT.",
      "required": true
    }
  }
}
